import { CustomTimeValidatorDirective } from './custom-time-validator.directive';

describe('CustomTimeValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomTimeValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
