import { HasTouchedAllDirective } from './has-touched-all.directive';

describe('HasTouchedAllDirective', () => {
  it('should create an instance', () => {
    const directive = new HasTouchedAllDirective();
    expect(directive).toBeTruthy();
  });
});
